name: Test with multiple sources
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          push: 'false'
          sources: |
            alpine:3.18
            busybox:1.36
      - name: Assert action completed with multiple sources
        run: |
          echo "Action completed successfully with multiple sources"
          echo "PASS"
