name: Test dry-run mode with multiple sources
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Docker Buildx
        run: docker buildx create --use --name test-builder-multi

      - id: action
        name: Run action in dry-run mode with multiple sources
        uses: .
        with:
          push: 'false'
          sources: |
            alpine:3.18
            alpine:3.17

      - name: Assert no digest output in dry-run mode with multiple sources
        env:
          DIGEST: ${{ steps.action.outputs.digest }}
        run: |
          echo "digest: '$DIGEST'"
          if [ -n "$DIGEST" ]; then
            echo "FAIL: expected no digest in dry-run mode with multiple sources, got: $DIGEST"
            exit 1
          fi
          echo "PASS: no digest output as expected in dry-run mode with multiple sources"
